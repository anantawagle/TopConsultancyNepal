export const dynamic = 'force-dynamic';
import type { Metadata } from 'next'
import Image from 'next/image'
import Link from 'next/link'
import { getConsultancies } from '@/lib/sanity/consultancies'
export const metadata: Metadata = { title: 'Consultancies', description: 'Browse education consultancies in Nepal.' }
export default async function ConsultanciesPage() { const consultancies = await getConsultancies(); return <main className="w-full flex-1 px-4 pb-24 pt-16"><div className="mx-auto max-w-6xl"><div className="mb-14 text-center"><p className="mb-3 text-sm font-bold uppercase tracking-widest text-brand-secondary">Consultancy directory</p><h1 className="text-4xl font-extrabold text-brand-primary md:text-5xl">Education Consultancies in Nepal</h1><p className="mx-auto mt-4 max-w-2xl text-lg text-text-muted">Compare services, destinations and public contact details before making your decision.</p></div>{consultancies.length ? <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{consultancies.map((item) => <article key={item._id} className="group flex flex-col rounded-2xl border border-slate-200 bg-white p-7 shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="mb-6 flex h-20 w-20 items-center justify-center overflow-hidden rounded-xl bg-slate-50">{item.logo?.url ? <Image src={item.logo.url} alt={item.logo.alt || `${item.name} logo`} width={80} height={80} className="h-full w-full object-contain p-2" /> : <span className="text-2xl font-bold text-brand-primary">{item.name.charAt(0)}</span>}</div><div className="flex items-center gap-2"><h2 className="text-xl font-bold text-brand-primary">{item.name}</h2>{item.isVerified && <span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">Verified</span>}</div><p className="mt-2 text-sm font-semibold text-brand-secondary">{item.city}</p><p className="mt-4 flex-1 text-sm leading-7 text-text-muted">{item.shortDescription}</p><Link href={`/consultancies/${item.slug}`} className="mt-6 font-bold text-brand-primary hover:text-brand-secondary">View profile →</Link></article>)}</div> : <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center"><h2 className="text-xl font-bold text-brand-primary">More consultancy profiles are coming soon</h2><p className="mt-2 text-text-muted">We are reviewing information to help you compare providers with confidence.</p></div>}</div></main> }
