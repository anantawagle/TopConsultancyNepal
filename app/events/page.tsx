export const dynamic = 'force-dynamic';
import type { Metadata } from 'next'
import Image from 'next/image'
import Link from 'next/link'
import { getUpcomingEvents } from '@/lib/sanity/events'
export const metadata: Metadata = { title: 'Upcoming Events', description: 'Discover education fairs, seminars and webinars.' }
const formatDate = new Intl.DateTimeFormat('en', { dateStyle: 'medium', timeStyle: 'short', timeZone: 'Asia/Kathmandu' })
export default async function EventsPage() { const events = await getUpcomingEvents(); return <main className="flex-1"><section className="bg-brand-primary text-white"><div className="mx-auto max-w-6xl px-4 py-16"><p className="mb-3 text-sm font-bold uppercase tracking-widest text-emerald-300">Meet, learn and connect</p><h1 className="text-4xl font-extrabold md:text-6xl">Upcoming Events</h1><p className="mt-5 max-w-3xl text-lg text-slate-200">Find education fairs, university sessions, test-preparation workshops and study-abroad webinars.</p></div></section><section className="mx-auto max-w-6xl px-4 py-14">{events.length ? <div className="grid gap-7 md:grid-cols-2 lg:grid-cols-3">{events.map((event) => <article key={event._id} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{event.image?.url ? <div className="relative aspect-video"><Image src={event.image.url} alt={event.image.alt || event.title} fill className="object-cover" /></div> : <div className="flex aspect-video items-center justify-center bg-brand-primary/5 text-5xl">📅</div>}<div className="p-6"><p className="text-sm font-bold text-brand-secondary">{formatDate.format(new Date(event.startDate))}</p><h2 className="mt-2 text-2xl font-bold text-brand-primary">{event.title}</h2><p className="mt-3 leading-7 text-text-muted">{event.summary}</p><Link href={`/events/${event.slug}`} className="mt-5 inline-block font-bold text-brand-secondary">View event →</Link></div></article>)}</div> : <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center"><h2 className="text-xl font-bold text-brand-primary">No upcoming events right now</h2><p className="mt-2 text-text-muted">Check back soon for education fairs, workshops and information sessions.</p></div>}</section></main> }
