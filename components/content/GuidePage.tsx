import Link from 'next/link'
import type { Guide } from '@/lib/sanity/queries'
import { RelatedConsultancies } from './RelatedConsultancies'
import { RelatedBlogs } from './RelatedBlogs'

export function GuidePage({ guide, topicPrefix, backHref, backLabel }: { guide: Guide, topicPrefix: string, backHref: string, backLabel: string }) {
  return <main className="flex-1"><section className="bg-brand-primary text-white"><div className="mx-auto max-w-6xl px-4 py-16 md:py-24"><Link href={backHref} className="mb-8 inline-block text-sm font-semibold text-white/75 hover:text-white">← {backLabel}</Link><p className="mb-3 text-sm font-bold uppercase tracking-[0.2em] text-emerald-300">{guide.eyebrow}</p><h1 className="max-w-4xl text-4xl font-extrabold tracking-tight md:text-6xl">{guide.name}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-200 md:text-xl">{guide.summary}</p></div></section><div className="mx-auto max-w-6xl px-4"><section className="grid gap-10 py-14 lg:grid-cols-[1.5fr_1fr]"><div><h2 className="mb-4 text-3xl font-bold text-brand-primary">What you should know</h2><p className="text-lg leading-8 text-text-muted">{guide.introduction}</p></div><dl className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">{guide.facts.map((fact) => <div key={fact.label} className="border-b border-slate-100 py-4 first:pt-0 last:border-0 last:pb-0"><dt className="text-sm font-semibold uppercase tracking-wide text-text-muted">{fact.label}</dt><dd className="mt-1 font-semibold text-text-main">{fact.value}</dd></div>)}</dl></section><section className="pb-14"><h2 className="mb-7 text-3xl font-bold text-brand-primary">Plan with confidence</h2><div className="grid gap-5 md:grid-cols-3">{guide.highlights.map((item, index) => <div key={item.title} className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span className="mb-5 flex h-10 w-10 items-center justify-center rounded-full bg-brand-secondary/10 font-bold text-brand-secondary">{index + 1}</span><h3 className="mb-2 text-xl font-bold text-brand-primary">{item.title}</h3><p className="leading-7 text-text-muted">{item.description}</p></div>)}</div></section>{topicPrefix === 'test' && <RelatedConsultancies testName={guide.name} />}<RelatedBlogs topics={[`${topicPrefix}:${guide.slug}`]} title={`Guides related to ${guide.name}`} /></div></main>
}
